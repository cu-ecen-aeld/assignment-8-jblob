name: assignment-test

on:
  push:
    branches:
      - '*'

jobs:
  full-test:
    runs-on: self-hosted

    container:
      image: cuaesd/aesd-autotest:24-assignment7-buildroot
      options: -v /home/jblob/yocto-cache:/yocto-cache -v /home/jblob/yocto-build:/build

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Checkout submodules
        run: git submodule update --init --recursive

      - name: Install dependencies + locale
        run: |
          apt-get update
          apt-get install -y locales python3-distutils gawk chrpath diffstat lz4 zstd unzip build-essential
          echo "en_US.UTF-8 UTF-8" > /etc/locale.gen
          locale-gen

      - name: Setup builder user and permissions
        run: |
          # User erstellen falls nicht vorhanden
          useradd -m builder || true
          
          # Build-Ordner VOR dem chown erstellen
          mkdir -p build/conf
          
          # Berechtigungen für den gesamten Workspace setzen
          # Dies behebt die 'Permission denied' Fehler bei .git und bitbake.lock
          chown -R builder:builder .
          
          # Optional: Falls der Cache-Mount Schreibrechte benötigt
          chown -R builder:builder /yocto-cache || true

      - name: Setup Yocto cache
        run: |
          su builder -c "
            echo 'DL_DIR = \"/yocto-cache/downloads\"' >> build/conf/local.conf
            echo 'SSTATE_DIR = \"/yocto-cache/sstate-cache\"' >> build/conf/local.conf
          "

      - name: Run full test
        run: |
          su builder -c "
            export LANG=en_US.UTF-8
            export LC_ALL=en_US.UTF-8
            export HOME=/home/builder
            ./full-test.sh
          "
