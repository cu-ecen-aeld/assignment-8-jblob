name: assignment-test

on:
  push:
    branches:
      - '*'

jobs:
  full-test:
    runs-on: self-hosted

    container:
      image: cuaesd/aesd-autotest:24-assignment7-buildroot
      options: -v /home/jblob/yocto-cache:/yocto-cache -v /home/jblob/yocto-build:/build

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Checkout submodules
        run: git submodule update --init --recursive

      - name: Install dependencies + locale
        run: |
          apt-get update
          apt-get install -y locales python3-distutils gawk chrpath diffstat lz4 zstd unzip build-essential
          echo "en_US.UTF-8 UTF-8" > /etc/locale.gen
          locale-gen

      - name: Setup builder user and persistent workspace
        run: |
          useradd -m builder || true
          rm -rf build
          ln -s /build build
          mkdir -p build/conf
          chown -R builder:builder build /build /yocto-cache || true

      - name: Setup Yocto cache and persistent TMPDIR
        run: |
          su builder -c "
            if [ ! -f build/conf/local.conf ]; then
              echo 'CONF_VERSION = \"2\"' > build/conf/local.conf
            fi

            sed -i '/DL_DIR/d' build/conf/local.conf
            sed -i '/SSTATE_DIR/d' build/conf/local.conf
            sed -i '/TMPDIR/d' build/conf/local.conf

            echo 'DL_DIR = \"/yocto-cache/downloads\"' >> build/conf/local.conf
            echo 'SSTATE_DIR = \"/yocto-cache/sstate-cache\"' >> build/conf/local.conf
            echo 'TMPDIR = \"/build/tmp\"' >> build/conf/local.conf
          "

      - name: Fix bblayers.conf (clean)
        run: |
          su builder -c '
            LAYER_PATH=$(realpath meta-aesd)
            sed -i "/meta-aesd/d" build/conf/bblayers.conf
            echo "BBLAYERS += \"${LAYER_PATH}\"" >> build/conf/bblayers.conf
          '

      # FINALER FIX: runqemu WRAPPER (NICHT überschreibbar)
      - name: Run full test with forced QEMU port
        run: |
          su builder -c "
            export LANG=en_US.UTF-8
            export LC_ALL=en_US.UTF-8
            export HOME=/home/builder

            mkdir -p \$HOME/bin

            cat << 'EOF' > \$HOME/bin/runqemu
#!/bin/bash

# echtes runqemu finden
REAL_RUNQEMU=\$(command -v runqemu)

echo \"[Wrapper] Forcing SSH port 10022\" >&2

# immer unseren Port anhängen
exec \$REAL_RUNQEMU \"\$@\" nographic slirp hostfwd=tcp::10022-:22
EOF

            chmod +x \$HOME/bin/runqemu

            # Wrapper priorisieren
            export PATH=\$HOME/bin:\$PATH

            ./full-test.sh
          "
``
